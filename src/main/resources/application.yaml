server:
  port: 8085

spring:
  application:
    name: library_service
  datasource:
    url: jdbc:mysql://localhost:3308/library_db3?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Dribbler@99
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

---
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:mysql://leedsdb:3306/library_db3?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Dribbler@99
