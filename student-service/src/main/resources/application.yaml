server:
  port: 8083

services:
  finance:
    url: http://localhost:8081
  library:
    url: http://localhost:8082/api/v1/mock-library # Port for CESBooks-master

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_portal?createDatabaseIfNotExist=true
    username: root
    password: Deadlyalive1@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  application:
    name: student-service